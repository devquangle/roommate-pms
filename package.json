{
  "name": "roommate-pms",
  "version": "1.0.0",
  "description": "RoomMate - Hệ thống quản lý nhà trọ và hóa đơn điện nước",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@vitest/ui": "^3.0.0",
    "jsdom": "^23.0.0",
    "vite": "^5.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "chart.js": "^4.0.0"
  }
}
