// src/pages/contracts/index.js

export function renderContractsPage(container) {
  container.innerHTML = `
    <div data-testid="contracts-page">
      <h4>Hợp đồng</h4>
      <p class="text-muted">Danh sách hợp đồng sẽ hiển thị ở đây.</p>
    </div>
  `;
}
